# Default Configuration (fallback)
s3.bucket.name=companies-data-bucket-default
app.environment=default
app.log.level=INFO

# Application Configuration
app.name=companies-details-lambda
app.version=1.0-SNAPSHOT

# Default settings
app.cache.enabled=false
app.metrics.enabled=false