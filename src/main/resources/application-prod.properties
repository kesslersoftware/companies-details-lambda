# Production Environment Configuration
s3.bucket.name=boycottpro-envs-prod
app.environment=prod
app.log.level=ERROR

# Production-specific settings
app.cache.enabled=true
app.metrics.enabled=true
app.performance.monitoring=true